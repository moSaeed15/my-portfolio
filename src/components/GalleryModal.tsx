interface Props {
  show: boolean;
  onClose: () => void;
  image: string[];
}

const GalleryModal = ({ show, onClose, image }: Props) => {
  return (
    <>
      <div></div>
    </>
  );
};

export default GalleryModal;
